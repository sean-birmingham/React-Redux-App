const initialState = {
    movies: ["Castle in the Sky", "My Neighbor Totoro", "Ponyo", "Spirited Away"]
}

export const movieReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}